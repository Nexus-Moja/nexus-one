services:
  nexus-one:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "4173:4173"
    volumes:
      - nexus_data:/app/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
volumes:
  nexus_data:
