# NEXUS ONE v1.0.5 Fix Notes

- Removed all query parameters from the Stripe hosted checkout link to prevent Stripe/AWS XML `AccessDenied` responses.
- Stripe buttons now open the exact hosted URL supplied for Nexus.
- Removed the language selector body fallback entirely.
- The selector can render only inside the navigation slot placed directly after Book a Ride.
- Added a CSS safeguard that hides any accidental body-level language selector.
- Added an i18n cache-busting version to static pages.
